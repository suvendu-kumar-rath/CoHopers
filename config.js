// Configuration file for URLs
const config = {
    FRONTEND_URL: 'https://co-hopers-frontend.vercel.app/',
    ABOUT_URL: 'https://co-hopers-frontend.vercel.app/about',
    CONTACT_URL: 'https://co-hopers-frontend.vercel.app/contact'
};

// Export for use in other scripts
if (typeof module !== 'undefined' && module.exports) {
    module.exports = config;
}
